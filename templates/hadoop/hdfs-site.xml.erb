<?xml version="1.0"?>
<!--
  Licensed to the Apache Software Foundation (ASF) under one or more
  contributor license agreements.  See the NOTICE file distributed with
  this work for additional information regarding copyright ownership.
  The ASF licenses this file to You under the Apache License, Version 2.0
  (the "License"); you may not use this file except in compliance with
  the License.  You may obtain a copy of the License at

      http://www.apache.org/licenses/LICENSE-2.0

  Unless required by applicable law or agreed to in writing, software
  distributed under the License is distributed on an "AS IS" BASIS,
  WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  See the License for the specific language governing permissions and
  limitations under the License.
-->
<?xml-stylesheet type="text/xsl" href="configuration.xsl"?>

<configuration>

  <property>
     <name>hadoop.tmp.dir</name>
     <value>/var/lib/hadoop-hdfs/${user.name}</value>
  </property>
  <property>
     <name>dfs.namenode.name.dir</name>
     <value><%= suffix = scope.lookupvar('hadoop::hdfs_namenode_suffix'); scope.lookupvar('hadoop::hdfs_name_dirs').map{ |dir| "file://#{dir}/#{suffix}" }.join(',') -%></value>
  </property>
  <property>
     <name>dfs.namenode.checkpoint.dir</name>
     <value><%= scope.lookupvar('hadoop::hdfs_name_dirs').map{ |dir| "file://#{dir}/${user.name}/dfs/secondarynamenode" }.join(',') -%></value>
  </property>
  <property>
     <name>dfs.datanode.data.dir</name>
     <value><%= scope.lookupvar('hadoop::hdfs_data_dirs').map{ |dir| "file://#{dir}/${user.name}/dfs/datanode" }.join(',') -%></value>
  </property>
<!--  <property>
     <name>dfs.journalnode.edits.dir</name>
     <value><%= scope.lookupvar('hadoop::hdfs_name_dirs').map{ |dir| "#{dir}/${user.name}/dfs/journalnode" }.join(',') -%></value>
  </property>-->
  <!-- permitted data nodes -->
  <property>
     <name>dfs.hosts</name>
     <value><%= scope.lookupvar('hadoop::confdir') -%>/slaves</value>
  </property>
  <!-- decommissioning of data nodes -->
  <property>
     <name>dfs.hosts.exclude</name>
     <value><%= scope.lookupvar('hadoop::confdir') -%>/excludes</value>
  </property>

<% if scope.lookupvar('hadoop::realm') and not scope.lookupvar('hadoop::realm').empty? -%>
<!--
  <property>
      <name>dfs.http.address</name>
      <value>0.0.0.0:50070</value>
  </property>
  <property>
      <name>dfs.datanode.ipc.address</name>
      <value>0.0.0.0:50020</value>
  </property>
-->
  <!-- secure mode -->
  <property>
    <name>dfs.namenode.keytab.file</name>
    <value>/etc/security/keytab/nn.service.keytab</value>
  </property>
  <property>
    <name>dfs.datanode.keytab.file</name>
    <value>/etc/security/keytab/dn.service.keytab</value>
  </property>
  <property>
    <name>dfs.journalnode.keytab.file</name>
    <value>/etc/security/keytab/jn.service.keytab</value>
  </property>
<% end -%>

<% hdfs_properties = scope.lookupvar('hadoop::props').select { |name,value| name =~ /^hadoop\.hdfs\.|^dfs\./ and value != '::undef' } -%>
<% hdfs_properties.keys.each do |p| -%>
<%if scope.lookupvar('hadoop::descs')[p] -%>
  <!-- <%= scope.lookupvar('hadoop::descs')[p] %> -->
<% end -%>
  <property>
    <name><%= p -%></name>
    <value><%= scope.lookupvar('hadoop::props')[p] -%></value>
  </property>
<% end -%>

</configuration>
